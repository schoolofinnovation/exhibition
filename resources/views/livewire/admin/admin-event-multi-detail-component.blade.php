<main>
    @if($formm == 'detailPavillion')
        <form wire:submit.prevent="">

        </form>
    @endif

    @if($formm == 'detailPavillion')
        <form wire:submit.prevent="">

        </form>
    @endif


</main>